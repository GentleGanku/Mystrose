<UserControl
    Loaded="OnLoaded"
    Unloaded="OnUnloaded"
    d:DesignHeight="30"
    d:DesignWidth="300"
    mc:Ignorable="d"
    x:Class="Mystrose.Views.WorldVisualizer.Controls.AttributeItem"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:baseControls="clr-namespace:Mystrose.Views.Base.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Mystrose.Views.WorldVisualizer.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Margin="8,4,8,12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock
            FontFamily="{StaticResource FF_HypatiaSans_Semibold}"
            FontSize="10"
            Foreground="{DynamicResource TextFillColorPrimaryBrush}"
            Grid.Row="0"
            Margin="0,0,0,2"
            Opacity="0.5"
            Text="Label"
            x:Name="TB_Label" />
        <ui:TextBox
            BorderBrush="{DynamicResource ControlElevationBorderBrush}"
            FontFamily="{StaticResource FF_HypatiaSans_Regular}"
            FontSize="12"
            Foreground="{DynamicResource TextFillColorPrimaryBrush}"
            Grid.Row="1"
            HorizontalAlignment="Stretch"
            IsReadOnly="True"
            PlaceholderText="(empty)"
            TextAlignment="Justify"
            TextWrapping="Wrap"
            x:Name="TBX_Content" />
    </Grid>

</UserControl>
