<UserControl
    x:Class="Mystrose.Views.Panels.General.FrozenPanel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    d:DesignHeight="540"
    d:DesignWidth="959"
    Background="{DynamicResource ApplicationBackgroundBrush}"
    mc:Ignorable="d">

    <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <Grid
            Grid.Column="0"
            Width="450"
            HorizontalAlignment="Center"
            VerticalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackPanel
                Grid.Row="0"
                Margin="5,0"
                HorizontalAlignment="Center"
                Orientation="Horizontal">
                <TextBlock
                    Margin="5"
                    Padding="0"
                    FontFamily="{StaticResource BDMerced}"
                    FontSize="32">
                    New Tab
                </TextBlock>
            </StackPanel>
            <Border
                Grid.Row="1"
                Width="500"
                Height="0.5"
                Margin="5,0"
                HorizontalAlignment="Center"
                Background="{DynamicResource SeparatorStrokeColorDefaultBrush}"
                CornerRadius="0.25"
                Opacity="1" />
            <TextBlock
                Grid.Row="2"
                Margin="10,7.5"
                FontFamily="{StaticResource Poppins}"
                FontSize="14"
                TextAlignment="Center"
                TextWrapping="Wrap">
                In the process of loading the game client on the new tab. Please wait...
            </TextBlock>
        </Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <Image
                Width="300"
                Height="300"
                Opacity="0.1"
                RenderOptions.BitmapScalingMode="HighQuality"
                SnapsToDevicePixels="True"
                Source="pack://application:,,,/Resources/Graphics/Mystrose.png"
                UseLayoutRounding="True" />
        </StackPanel>
    </Grid>

</UserControl>
