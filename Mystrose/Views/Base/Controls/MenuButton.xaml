<UserControl
    x:Class="Mystrose.Views.Base.Controls.MenuButton"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Mystrose.Views.Base.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    d:DesignHeight="32"
    d:DesignWidth="32"
    mc:Ignorable="d">

    <ui:Button
        x:Name="BTN_Item"
        Width="32"
        Height="32"
        Padding="8,0,0,0"
        HorizontalContentAlignment="Left"
        Background="Transparent"
        BorderBrush="Transparent">

        <StackPanel Orientation="Horizontal">
            <ContentPresenter
                x:Name="CPST_Icon"
                Width="14"
                Height="14"
                Margin="0,0,3,0" />
            <TextBlock
                x:Name="TB_Caption"
                Height="14"
                Padding="5,0"
                VerticalAlignment="Center"
                FontFamily="{StaticResource FF_Albertus_Light}"
                FontSize="12"
                Opacity="0">
                About
            </TextBlock>
        </StackPanel>

        <ui:Button.Triggers>
            <EventTrigger RoutedEvent="Button.MouseEnter" SourceName="BTN_Item">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation
                            Storyboard.TargetName="BTN_Item"
                            Storyboard.TargetProperty="Width"
                            From="32"
                            To="{Binding FullButtonWidth}"
                            Duration="00:00:00.15" />
                        <DoubleAnimation
                            Storyboard.TargetName="TB_Caption"
                            Storyboard.TargetProperty="Opacity"
                            From="0"
                            To="1"
                            Duration="00:00:00.15" />
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
            <EventTrigger RoutedEvent="Button.MouseLeave" SourceName="BTN_Item">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation
                            Storyboard.TargetName="BTN_Item"
                            Storyboard.TargetProperty="Width"
                            From="{Binding FullButtonWidth}"
                            To="32"
                            Duration="00:00:00.15" />
                        <DoubleAnimation
                            Storyboard.TargetName="TB_Caption"
                            Storyboard.TargetProperty="Opacity"
                            From="1"
                            To="0"
                            Duration="00:00:00.15" />
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </ui:Button.Triggers>
    </ui:Button>

</UserControl>
